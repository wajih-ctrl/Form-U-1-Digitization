import { defineConfig } from '@playwright/test'
export default defineConfig({ testDir: './tests', timeout: 60000, expect: { timeout: 10000 }, use: { baseURL: process.env.QA_BASE_URL ?? 'http://localhost:3000', viewport: { width: 1440, height: 1000 }, screenshot: 'only-on-failure', trace: 'retain-on-failure' }, workers: 1, reporter: [['list'], ['json', { outputFile: 'qa-artifacts/tests.json' }]] })
